apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Example: override image tags per environment via 'images:' here,
# or patch replica counts with patchesStrategicMerge for dev/staging/prod.
