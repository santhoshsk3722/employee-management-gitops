apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: employee-mgmt
  labels:
    app: api-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: <ECR_REGISTRY>/employee-mgmt/api-gateway:<IMAGE_TAG>
          ports:
            - containerPort: 8080
          env:
            - name: EUREKA_URI
              value: "http://eureka-server:8761/eureka"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
