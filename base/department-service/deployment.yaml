apiVersion: apps/v1
kind: Deployment
metadata:
  name: department-service
  namespace: employee-mgmt
  labels:
    app: department-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: department-service
  template:
    metadata:
      labels:
        app: department-service
    spec:
      containers:
        - name: department-service
          image: <ECR_REGISTRY>/employee-mgmt/department-service:<IMAGE_TAG>
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: department-service-config
          resources:
            requests:
              cpu: 150m
              memory: 384Mi
            limits:
              cpu: 750m
              memory: 768Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 25
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 40
            periodSeconds: 15
