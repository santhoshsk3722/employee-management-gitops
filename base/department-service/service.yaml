apiVersion: v1
kind: Service
metadata:
  name: department-service
  namespace: employee-mgmt
  labels:
    app: department-service
spec:
  selector:
    app: department-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  type: ClusterIP
